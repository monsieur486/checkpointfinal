{% if is_granted('IS_AUTHENTICATED_FULLY') %}
    {% if is_granted('ROLE_ADMIN') %}
        <li class="{% if activePage=="admin" %}uk-active{% endif %}">
            <a href="{{ path('administration') }}">Administration</a>
        </li>
    {% endif %}
    <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
{% else %}
    <li class="{% if activePage=="login" %}uk-active{% endif %}">
        <a  href="{{ path('app_login')}}"><span class="fas fa-user-lock" uk-tooltip="Connexion"></span></a>
    </li>
{% endif %}
