{% extends "base.html.twig" %}

{% block title %}-* WildCircus *-{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('app') }}
{% endblock %}

{% block body %}
    {% include 'nav.html.twig' %}
    <div class="uk-container uk-container-expand">
        <section class="firstsection">
            <div class="uk-child-width-1-3@s uk-text-center" uk-grid>
                <div>
                    <li class="{% if activePage=="BOTarifs" %}uk-active{% else %}degrade{% endif %}">
                        <a  href="{{ path('BOTarifs')}}">
                            <div class="uk-box-shadow-small uk-box-shadow-hover-large uk-padding-small">
                                Tarifs
                            </div>
                        </a>
                    </li>
                </div>
                <div>
                    <li class="{% if activePage=="BOCircuit" %}uk-active{% else %}degrade{% endif %}">
                        <a  href="{{ path('BOCircuit')}}">
                            <div class="uk-box-shadow-small uk-box-shadow-hover-large uk-padding-small">
                                Circuit
                            </div>
                        </a>
                    </li>
                </div>
                <div>
                    <li class="{% if activePage=="BOProgramme" %}uk-active{% else %}degrade{% endif %}">
                        <a  href="{{ path('BOProgramme')}}">
                            <div class="uk-box-shadow-small uk-box-shadow-hover-large uk-padding-small">
                                Programme
                            </div>
                        </a>
                    </li>
                </div>
            </div>
        </section>
        <section>
            <h1>Programme</h1>
        </section>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('app') }}
{% endblock %}
